files:

  "/tmp/install-nginx-config.sh" :
    mode: "000755"
    owner: root
    group: root
    content: |
      #!/bin/sh
      cp /tmp/custom.nginx.conf /tmp/deployment/config/#etc#nginx#conf.d#00_application.conf
